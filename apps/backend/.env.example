PROJECT_NAME="EduConnect"
API_V1_STR="/api/v1"
ENVIRONMENT="development"
HOST="0.0.0.0"
PORT=8000
LOG_LEVEL="DEBUG"

# Database placeholders — copy to `.env` and replace with real values.
DATABASE_URL="postgresql+asyncpg://user:replace-me@localhost:5432/educonnect"
REDIS_URL="redis://:replace-me@localhost:6379/0"
